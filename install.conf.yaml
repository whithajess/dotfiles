- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zsh/zshrc
    ~/.gitconfig: git/gitconfig
    ~/.gitignore_global: git/gitignore_global
    ~/Loyalty/.gitconfig_loyalty: git/gitconfig_loyalty

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [./fonts/install.sh]
